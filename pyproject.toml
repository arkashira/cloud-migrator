[project]
name = "cloud-migrator"
version = "0.1.0"
description = "Simple task cost/effort estimator for cloud migration planning."
authors = [{name = "Axentx AI"}]
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
